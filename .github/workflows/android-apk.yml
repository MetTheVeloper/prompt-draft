name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-debug-apk:
    name: Build debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10 --activate

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Show versions
        run: |
          node -v
          pnpm -v
          java -version

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Nuxt static output
        run: pnpm generate

      - name: Generate Android app assets
        run: pnpm exec capacitor-assets generate android --assetPath assets --androidProject android --iconBackgroundColor '#0f0f14' --iconBackgroundColorDark '#0f0f14' --splashBackgroundColor '#0f0f14' --splashBackgroundColorDark '#0f0f14'

      - name: Accept Android SDK licenses
        run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true

      - name: Install Android SDK platforms
        run: |
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-35"

      - name: Sync Capacitor Android
        run: npx cap sync android

      - name: Make Gradle executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: prompt-draft-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7