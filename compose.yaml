services:
  api:
    build:
      context: ./backend
    ports:
      - "4000:4000"
    environment:
      HOST: 0.0.0.0
      PORT: 4000
      CORS_ORIGINS: "http://localhost:3030,http://127.0.0.1:3030"
